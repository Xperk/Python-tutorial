# Text Type:	    str
# Numeric Types:	int, float, complex
# Sequence Types:	list, tuple, range
# Mapping Type:	    dict
# Set Types:	    set, frozenset
# Boolean Type:	    bool
# Binary Types:	    bytes, bytearray, memoryview
# None Type:	    NoneType

# list            Được sắp xếp và thay đổi, có thể trùng lặp               
# tuple           Được sắp xếp theo thứ tự và không thể thay đổi, có thể trùng lặp
# set             Không có thứ tự, không thể thay đổi, không được lập chỉ mục, không trùng lặp
# dictionary      Được sắp xếp theo thứ tự và có thể thay đổi, không trùng lặp